{
  "name": "transaction-anomaly-visualizer",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "dashboard",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w dashboard\" \"npm start -w tav-backend\"",
    "build": "npm run build -w dashboard",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
